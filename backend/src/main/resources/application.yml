server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wms_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: '!yzj20050626'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  sql:
    init:
      mode: always
      continue-on-error: true

jwt:
  secret: WmsJwtSecretKey2026WarehouseManagementSystemByExample
  expiration: 86400000
  header: Authorization

logging:
  level:
    com.example.wms: debug